import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'algerian.services.marketplace',
  appName: 'algerian-services-marketplace',
  webDir: 'www'
};

export default config;
